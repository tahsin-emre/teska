import 'package:flutter/material.dart';
import 'package:teska/constants/app_sizes.dart';

final class AppPaddings {
  const AppPaddings._();

  static EdgeInsets get allXXS => const EdgeInsets.all(AppSizes.xxs);
  static EdgeInsets get allXS => const EdgeInsets.all(AppSizes.xs);
  static EdgeInsets get allS => const EdgeInsets.all(AppSizes.s);
  static EdgeInsets get allM => const EdgeInsets.all(AppSizes.m);
  static EdgeInsets get allL => const EdgeInsets.all(AppSizes.l);
  static EdgeInsets get allXL => const EdgeInsets.all(AppSizes.xl);
  static EdgeInsets get allXXL => const EdgeInsets.all(AppSizes.xxl);
}
